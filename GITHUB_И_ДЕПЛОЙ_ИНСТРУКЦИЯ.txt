================================================================================
        AGENTPULSE - ПОЛНАЯ ИНСТРУКЦИЯ ПО GITHUB И ДЕПЛОЮ НА СЕРВЕР
================================================================================

Дата создания: 18 февраля 2026
Автор: AgentPulse Team

================================================================================
СОДЕРЖАНИЕ
================================================================================

1. Что такое GitHub и зачем он нужен
2. Что мы уже сделали (краткая сводка)
3. Основные команды Git (шпаргалка)
4. Как работать с GitHub при разработке
5. Деплой на VPS сервер (пошаговая инструкция)
6. Обновление кода на сервере
7. Полезные команды для сервера
8. Решение проблем

================================================================================
1. ЧТО ТАКОЕ GITHUB И ЗАЧЕМ ОН НУЖЕН
================================================================================

GitHub - это облачное хранилище для кода. Представь его как "Google Drive для программистов".

ЗАЧЕМ ОН НУЖЕН:

✓ Быстрый деплой на сервер
  Вместо копирования файлов через WinSCP (10 минут), просто пишешь:
  git clone https://github.com/AndreyP55/AgentPulse.git
  И весь проект скачивается за 5 секунд!

✓ Легкие обновления
  Изменил код локально → git push (3 секунды)
  На сервере → git pull (3 секунды)
  Без GitHub пришлось бы заново копировать файлы вручную.

✓ Резервная копия
  Если компьютер сломается - весь код сохранён на GitHub.
  Можешь скачать его с любого компьютера в любое время.

✓ История изменений
  GitHub хранит ВСЕ версии твоего кода.
  Если что-то сломал - можешь откатиться к рабочей версии.

✓ Работа с разных мест
  Можешь работать с кодом дома, на работе, у друга.
  Просто склонируй репозиторий на любой компьютер.

БЕЗОПАСНОСТЬ:
Твой репозиторий ПРИВАТНЫЙ - никто кроме тебя не видит код.
Файл config.json (с API ключами) НЕ загружается на GitHub - он защищён!

================================================================================
2. ЧТО МЫ УЖЕ СДЕЛАЛИ
================================================================================

✓ Установили Git на компьютере
✓ Создали .gitignore (защита приватных данных)
✓ Инициализировали Git репозиторий
✓ Создали первый коммит (сохранили код)
✓ Создали приватный репозиторий на GitHub
✓ Загрузили код на GitHub

ТВОЙ РЕПОЗИТОРИЙ:
https://github.com/AndreyP55/AgentPulse

Можешь открыть эту ссылку в браузере и увидеть свой код!

ЧТО ЗАЩИЩЕНО (не попадёт на GitHub):
- openclaw-acp/config.json (API ключи, токены)
- openclaw-acp/node_modules/ (зависимости)
- *.env файлы
- Логи

================================================================================
3. ОСНОВНЫЕ КОМАНДЫ GIT (ШПАРГАЛКА)
================================================================================

НА СВОЁМ КОМПЬЮТЕРЕ (Windows):

1. Посмотреть статус (что изменилось):
   cd c:\Users\Pc\Desktop\AgentPulse
   git status

2. Добавить все изменения:
   git add .

3. Сохранить изменения с описанием:
   git commit -m "Описание того, что изменил"

4. Загрузить на GitHub:
   git push

5. Скачать изменения с GitHub (если работаешь с другого компьютера):
   git pull

НА СЕРВЕРЕ (Linux):

1. Скачать обновления с GitHub:
   cd /root/AgentPulse
   git pull

2. Перезапустить агента после обновления:
   pm2 restart agentpulse-seller

3. Посмотреть логи:
   pm2 logs agentpulse-seller

================================================================================
4. КАК РАБОТАТЬ С GITHUB ПРИ РАЗРАБОТКЕ
================================================================================

ТИПИЧНЫЙ РАБОЧИЙ ПРОЦЕСС:

Шаг 1: Изменяешь код локально (на своём компьютере)
   - Открываешь Cursor
   - Редактируешь файлы
   - Тестируешь локально

Шаг 2: Сохраняешь изменения в Git
   cd c:\Users\Pc\Desktop\AgentPulse
   git add .
   git commit -m "Добавил новый offering"

Шаг 3: Загружаешь на GitHub
   git push

Шаг 4: Обновляешь на сервере
   ssh root@твой-сервер-ip
   cd /root/AgentPulse
   git pull
   pm2 restart agentpulse-seller

ГОТОВО! Изменения работают на сервере!


ПРИМЕР: ДОБАВЛЯЕМ НОВЫЙ OFFERING

1. Локально создаёшь новую папку:
   c:\Users\Pc\Desktop\AgentPulse\openclaw-acp\src\seller\offerings\agentpulse\uptime_monitor_24h\

2. Создаёшь файлы:
   - offering.json
   - handlers.ts

3. Тестируешь локально:
   cd openclaw-acp
   npx tsx src\seller\runtime\seller.ts

4. Сохраняешь в Git:
   cd c:\Users\Pc\Desktop\AgentPulse
   git add .
   git commit -m "Add uptime_monitor_24h offering"
   git push

5. Обновляешь на сервере:
   ssh root@твой-сервер
   cd /root/AgentPulse
   git pull
   cd openclaw-acp
   npx tsx bin/acp.ts sell create
   pm2 restart agentpulse-seller


ПРИМЕР: ИСПРАВЛЯЕМ БАГ В КОДЕ

1. Локально исправляешь код в Cursor

2. Тестируешь:
   cd openclaw-acp
   npx tsx src\seller\runtime\seller.ts

3. Сохраняешь:
   cd c:\Users\Pc\Desktop\AgentPulse
   git add .
   git commit -m "Fix bug in health_check handler"
   git push

4. Обновляешь на сервере:
   ssh root@твой-сервер
   cd /root/AgentPulse
   git pull
   pm2 restart agentpulse-seller

================================================================================
5. ДЕПЛОЙ НА VPS СЕРВЕР (ПОШАГОВАЯ ИНСТРУКЦИЯ)
================================================================================

ШАГ 1: ВЫБОР И ЗАКАЗ СЕРВЕРА
------------------------------------------------------------

Рекомендуемая конфигурация для AgentPulse:

Параметр         | Минимум    | Рекомендуется
-----------------+------------+---------------
CPU              | 1 vCPU     | 2 vCPU
RAM              | 1 GB       | 2 GB
Диск             | 10 GB      | 20 GB
ОС               | Ubuntu 22.04 | Ubuntu 24.04
Цена             | ~$2-3/мес  | ~$4-5/мес

На VDSina (https://www.vdsina.com/):
- Выбери "Стандартный VPS/VDS сервер"
- Локация: Амстердам, Нидерланды
- ОС: Ubuntu 22.04 или 24.04 LTS
- Тариф: от $2.10/месяц

После заказа получишь:
- IP-адрес сервера (например: 185.123.45.67)
- Логин: root
- Пароль для входа


ШАГ 2: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ
------------------------------------------------------------

Через PowerShell (Windows):

1. Открой PowerShell
2. Введи команду:
   ssh root@185.123.45.67
   (замени 185.123.45.67 на IP твоего сервера)
3. Введи пароль (при вводе символы не отображаются - это нормально)
4. Нажми Enter

Если PowerShell не работает, используй PuTTY:
1. Скачай PuTTY: https://www.putty.org/
2. Введи IP сервера
3. Нажми "Open"
4. Введи логин: root
5. Введи пароль


ШАГ 3: НАСТРОЙКА СЕРВЕРА
------------------------------------------------------------

После подключения выполни команды по порядку:

1. Обновление системы:
   apt update && apt upgrade -y

2. Установка Node.js 20:
   curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
   apt install -y nodejs

3. Проверка установки Node.js:
   node --version
   (должно показать v20.x.x)

4. Установка PM2 (для автозапуска):
   npm install -g pm2

5. Установка Git:
   apt install -y git

6. Проверка Git:
   git --version


ШАГ 4: СКАЧИВАНИЕ ПРОЕКТА С GITHUB
------------------------------------------------------------

1. Перейди в домашнюю папку:
   cd /root

2. Склонируй репозиторий:
   git clone https://github.com/AndreyP55/AgentPulse.git

3. Перейди в папку проекта:
   cd AgentPulse/openclaw-acp

4. Установи зависимости:
   npm install

5. Настрой агента:
   npm run setup

   Тебя спросят:
   - API Key агента: <YOUR_API_KEY>
   - Остальное можно оставить по умолчанию


ШАГ 5: ЗАПУСК ЧЕРЕЗ PM2 (24/7)
------------------------------------------------------------

1. Перейди в папку openclaw-acp:
   cd /root/AgentPulse/openclaw-acp

2. Запусти seller runtime через PM2:
   pm2 start "npx tsx src/seller/runtime/seller.ts" --name agentpulse-seller

3. Сохрани конфигурацию PM2:
   pm2 save

4. Настрой автозапуск при перезагрузке сервера:
   pm2 startup
   (скопируй и выполни команду, которую PM2 выдаст)

5. Проверь статус:
   pm2 status

   Должно быть:
   ┌─────┬────────────────────┬─────────┬─────────┐
   │ id  │ name               │ status  │ restart │
   ├─────┼────────────────────┼─────────┼─────────┤
   │ 0   │ agentpulse-seller  │ online  │ 0       │
   └─────┴────────────────────┴─────────┴─────────┘


ШАГ 6: ПРОВЕРКА РАБОТЫ
------------------------------------------------------------

1. Посмотри логи в реальном времени:
   pm2 logs agentpulse-seller

   Должно быть:
   [seller] Agent: AgentPulse (dir: agentpulse)
   [seller] Available offerings: health_check, reputation_report
   [seller] Seller runtime is running. Waiting for jobs...
   [socket] Connected to ACP
   [socket] Joined ACP room

2. Создай тестовый заказ в Butler (app.virtuals.io)

3. Проверь, что джоба выполнилась в логах

ГОТОВО! Агент работает 24/7! 🎉

================================================================================
6. ОБНОВЛЕНИЕ КОДА НА СЕРВЕРЕ
================================================================================

Когда ты изменил код локально и загрузил на GitHub:

1. Подключись к серверу:
   ssh root@твой-сервер-ip

2. Перейди в папку проекта:
   cd /root/AgentPulse

3. Скачай обновления:
   git pull

4. Если добавлял новые зависимости:
   cd openclaw-acp
   npm install

5. Если добавлял новые offerings:
   npx tsx bin/acp.ts sell create

6. Перезапусти агента:
   pm2 restart agentpulse-seller

7. Проверь логи:
   pm2 logs agentpulse-seller

ГОТОВО! Обновления применены!

================================================================================
7. ПОЛЕЗНЫЕ КОМАНДЫ ДЛЯ СЕРВЕРА
================================================================================

УПРАВЛЕНИЕ PM2:
------------------------------------------------------------
pm2 status                      - Статус всех процессов
pm2 logs agentpulse-seller      - Логи в реальном времени
pm2 logs agentpulse-seller --lines 100  - Последние 100 строк логов
pm2 stop agentpulse-seller      - Остановить агента
pm2 start agentpulse-seller     - Запустить агента
pm2 restart agentpulse-seller   - Перезапустить агента
pm2 delete agentpulse-seller    - Удалить из PM2
pm2 monit                       - Мониторинг ресурсов (CPU, RAM)


РАБОТА С GIT НА СЕРВЕРЕ:
------------------------------------------------------------
cd /root/AgentPulse
git status                      - Статус репозитория
git pull                        - Скачать обновления
git log --oneline -10           - Последние 10 коммитов


РАБОТА С OPENCLAW-ACP:
------------------------------------------------------------
cd /root/AgentPulse/openclaw-acp
npx tsx bin/acp.ts sell list    - Список offerings
npx tsx bin/acp.ts sell create  - Создать/обновить offerings
npm install                     - Установить зависимости


СИСТЕМНЫЕ КОМАНДЫ:
------------------------------------------------------------
df -h                           - Свободное место на диске
free -h                         - Использование RAM
top                             - Мониторинг процессов (выход: q)
htop                            - Улучшенный мониторинг (установка: apt install htop)
reboot                          - Перезагрузка сервера


БЕЗОПАСНОСТЬ:
------------------------------------------------------------
passwd                          - Сменить пароль root
apt install -y ufw              - Установить firewall
ufw allow 22/tcp                - Разрешить SSH
ufw enable                      - Включить firewall

================================================================================
8. РЕШЕНИЕ ПРОБЛЕМ
================================================================================

ПРОБЛЕМА: Агент не запускается
------------------------------------------------------------
Решение:
1. Проверь логи:
   pm2 logs agentpulse-seller

2. Проверь, что config.json существует:
   cat /root/AgentPulse/openclaw-acp/config.json

3. Если config.json нет, создай его:
   cd /root/AgentPulse/openclaw-acp
   npm run setup


ПРОБЛЕМА: git pull выдаёт ошибку
------------------------------------------------------------
Решение:
1. Проверь статус:
   git status

2. Если есть локальные изменения, сбрось их:
   git reset --hard
   git pull


ПРОБЛЕМА: Джобы не выполняются
------------------------------------------------------------
Решение:
1. Проверь, что агент подключён к ACP:
   pm2 logs agentpulse-seller
   Должно быть: [socket] Connected to ACP

2. Проверь offerings:
   cd /root/AgentPulse/openclaw-acp
   npx tsx bin/acp.ts sell list

3. Перезапусти агента:
   pm2 restart agentpulse-seller


ПРОБЛЕМА: Нет места на диске
------------------------------------------------------------
Решение:
1. Проверь использование:
   df -h

2. Очисти логи PM2:
   pm2 flush

3. Очисти кэш npm:
   npm cache clean --force


ПРОБЛЕМА: Забыл пароль от сервера
------------------------------------------------------------
Решение:
1. Зайди в панель управления VDSina
2. Найди свой сервер
3. Используй функцию "Сбросить пароль" или "VNC консоль"

================================================================================
БЫСТРАЯ ШПАРГАЛКА
================================================================================

НА КОМПЬЮТЕРЕ (Windows):
------------------------------------------------------------
cd c:\Users\Pc\Desktop\AgentPulse
git status                      # Что изменилось
git add .                       # Добавить изменения
git commit -m "Описание"        # Сохранить
git push                        # Загрузить на GitHub


НА СЕРВЕРЕ (Linux):
------------------------------------------------------------
ssh root@твой-ip                # Подключиться
cd /root/AgentPulse             # Перейти в проект
git pull                        # Скачать обновления
pm2 restart agentpulse-seller   # Перезапустить
pm2 logs agentpulse-seller      # Посмотреть логи


ПОЛЕЗНЫЕ ССЫЛКИ:
------------------------------------------------------------
GitHub репозиторий:     https://github.com/AndreyP55/AgentPulse
VDSina хостинг:         https://www.vdsina.com/
Virtuals Butler:        https://app.virtuals.io
aGDP.io:                https://agdp.io

================================================================================
КОНТАКТЫ И ПОДДЕРЖКА
================================================================================

Если что-то не работает:
1. Проверь логи: pm2 logs agentpulse-seller
2. Перечитай инструкцию
3. Попробуй перезапустить: pm2 restart agentpulse-seller

Документация проекта:
- MASTER_GUIDE.md - Полное руководство
- AGENTPULSE_SPEC.md - Спецификация агента
- AGENTPULSE_SESSION_REPORT.md - Отчёт по разработке

================================================================================
КОНЕЦ ИНСТРУКЦИИ
================================================================================

Удачи с деплоем! 🚀

Последнее обновление: 18 февраля 2026
