# Optional: paste token contract address after Virtuals launch.
# Then add the same in Vercel: Project → Settings → Environment Variables.
# NEXT_PUBLIC_TOKEN_CONTRACT=0x...

# Optional: secret token for webhook authentication.
# Set the same value in the seller's WEBHOOK_SECRET env var.
# WEBHOOK_SECRET=your-secret-token-here
